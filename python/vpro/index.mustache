<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title}}</title>
    <style>
      img {
        max-width: 100%;
        background-color: white;
      }
      body {
        background-color: #eee;
        margin: 0;
        padding: 0;
      }
      h1, h2, p {
        margin: 0;
        padding: 1ex;
      }
      h1 {
        background-color: deepskyblue;
        border-bottom: darkblue dotted 2px;
        color: white;
      }
      h2 {
        margin-top: 1ex;
        background-color: gold;
        border-bottom: black dotted 2px;
        color: black;
      }
      a {
        color: black;
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <h1>{{title}}</h1>
    <p><a href="..">UP</a></p>
    {{#svgs}}
      <h2>{{file_name}} {{#profile}} profile: {{profile}}{{/profile}}</h2>
      {{#txt_file}}
        <p>
          <a href="{{txt_file}}">URLs</a>
        </p>
      {{/txt_file}}
      <img src="{{ svg }}" alt="{{file_name}}"/>
    {{/svgs}}
  </body>
</html>